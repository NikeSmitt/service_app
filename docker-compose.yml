services:
  web-app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
#      необходимо чтобы не билдить изменения в коде каждый раз
      - ./service:/service

    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
